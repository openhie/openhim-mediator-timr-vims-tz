Running the mediator and logs location
======================================

The mediator can be manually run using below commands

.. code-block:: bash

   cd openhim-mediator-timr-vims-tz
   node index.js

Logs are located under /var/log/timr_vims, error.log file has all the mediator errors while info.log has all the other transaction details

